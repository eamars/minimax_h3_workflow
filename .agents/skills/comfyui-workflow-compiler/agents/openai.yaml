interface:
  display_name: "ComfyUI Workflow Compiler"
  short_description: "Compile ready plans into validated ComfyUI jobs"
  default_prompt: "Use $comfyui-workflow-compiler to compile this ready production-plan revision into validated ComfyUI workflows and a production DAG without queueing renders."
