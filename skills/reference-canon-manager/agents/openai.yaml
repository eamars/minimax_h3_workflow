interface:
  display_name: "Reference & Canon Manager"
  short_description: "Lock seed assets into traceable canon"
  default_prompt: "Use $reference-canon-manager to map these seed assets into a conflict-aware canon lock for the video plan."
policy:
  allow_implicit_invocation: true
