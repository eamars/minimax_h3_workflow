interface:
  display_name: "Keyframe & Handoff Builder"
  short_description: "Prepare approved endpoint frames and handoffs"
  default_prompt: "Use $keyframe-handoff-builder to prepare exact endpoint frames and approved continuation or bridge handoffs."
policy:
  allow_implicit_invocation: true
