interface:
  display_name: "Scene & Performance Writer"
  short_description: "Write playable behavior and performance continuity"
  default_prompt: "Use $scene-performance-writer to turn the validated plot and canon into playable scene performance with dialogue, reactions, and continuity."
policy:
  allow_implicit_invocation: true
